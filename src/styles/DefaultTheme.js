import { createTheme } from '@rneui/themed';

export default createTheme({
    lightColors: {
        primary: 'blue',
    },
    darkColors: {
        primary: '#000',
    },
    mode: 'light',
});
  